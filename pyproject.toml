[project]
name = "vfm"
version = "1.0.0"
description = "Visual File Manager"
dependencies = [
    "rich",
    "typer",
    "fastapi",
    "uvicorn",
    "plotly"
]

[project.scripts]
vfm = "vfm.cli:main"
